# Ruff — Python linter (optional: make lint-backend)
target-version = "py311"
line-length = 100

[lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]

[lint.per-file-ignores]
"app.py" = ["F401", "F841"]
